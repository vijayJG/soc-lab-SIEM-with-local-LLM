# Sample Output - LLM Analysis of Wazuh Alert

This is a real output from the SOCAI engine analyzing a brute-force login alert generated by Wazuh.

## Input Alert

```
Agent: DESKTOP-D30007T
Alert: Multiple failed login attempts detected
```

## LLM Response (Llama 3.1 8B via Ollama)

```
(socai-env) vijay@ubuntu:~$ python3 socai.py

--- SOC AI RESPONSE ---
```

**Severity: Medium**

The severity level is Medium because multiple failed SSH login attempts can indicate a brute-force attack or an attempt to gain unauthorized access to the system. While not immediately catastrophic, it warrants further investigation and mitigation.

**Attack Explanation**

A malicious actor is attempting to guess or crack the SSH login credentials of the target system (DESKTOP-D30007T). This can be done through password cracking tools or automated brute-force attacks. The goal is likely to gain unauthorized access, potentially leading to data theft, malware deployment, or further lateral movement.

**MITRE ATT&CK Mapping**

- T1056 - Input Capture (attempting to guess or crack login credentials)
- T1550 - Use Alternate Authentication Material (exploiting the SSH service)

**Recommended Actions**

1. Investigate the source IP address - identify the IPs attempting to connect and analyze their behavior to determine if the attack is targeted or opportunistic.
2. Implement rate limiting - configure the SSH server to limit login attempts within a time window to slow brute-force attempts.
3. Monitor system logs - continuously monitor Wazuh alerts for similar incidents to identify ongoing or escalating attacks.
4. Enforce strong password policies - ensure users have strong, unique passwords and consider enabling SSH key-based authentication.
5. Update security measures - consider enabling IP blocking, account lockout policies, or two-factor authentication.
6. Notify affected users - if a specific user's credentials are being targeted, notify them and advise a password reset.

Further investigation may be necessary to determine the root cause and prevent future incidents.
